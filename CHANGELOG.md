# ChangeLog

## Release_1.1.1_20251102_build_A

### 功能构建

- Wiki 更新。
  - docs/wiki/zh-CN/VersionBlacklist.md。

- 依赖升级。
  - 升级 `subgrade` 依赖版本为 `1.7.1.a` 以规避漏洞。

### Bug 修复

- (无)

### 功能移除

- (无)

---

## Release_1.1.0_20251027_build_A

### 功能构建

- Wiki 编写。
  - docs/wiki/zh-CN/ResourceSupport.md。

- 依赖升级。
  - 升级 `subgrade` 依赖版本为 `1.7.0.a` 以规避漏洞。

- 优化开发环境支持。
  - 在 .gitignore 中添加 VSCode 相关文件的忽略规则。
  - 在 .gitignore 中添加 Cursor IDE 相关文件的忽略规则。

### Bug 修复

- (无)

### 功能移除

- (无)

---

## Release_1.0.4_20251012_build_A

### 功能构建

- 优化 `docs/wiki` 目录结构。
  - 将 `docs/wiki/en_US` 目录重命名为 `en-US`，以符合 rfc5646 规范。
  - 将 `docs/wiki/zh_CN` 目录重命名为 `zh-CN`，以符合 rfc5646 规范。
  - 更新 `docs/wiki/README.md` 中的链接指向。
  - 更新 `README.md` 中的链接指向。

- 依赖升级。
  - 增加依赖 `subgrade` 以规避漏洞，版本为 `1.6.0.a`。
  - 增加依赖 `spring-telqos` 以规避漏洞，版本为 `1.1.14.a`。

### Bug 修复

- (无)

### 功能移除

- (无)

---

## Release_1.0.3_20250803_build_A

### 功能构建

- Wiki 编写。
  - docs/wiki/zh_CN/InstallBySourceCode.md。

- 依赖升级。
  - 升级 `commons-lang3` 依赖版本为 `3.18.0` 以规避漏洞。
  - 升级 `subgrade` 依赖版本为 `1.5.11.a` 以规避漏洞。

### Bug 修复

- (无)

### 功能移除

- (无)

---

## Release_1.0.2_20250605_build_A

### 功能构建

- Wiki 编写。
  - docs/wiki/zh_CN/VersionBlacklist.md。

- 依赖升级。
  - 升级 `commons-beanutils` 依赖版本为 `1.11.0` 以规避漏洞。
  - 升级 `subgrade` 依赖版本为 `1.5.10.a` 以规避漏洞。
  - 升级 `spring-telqos` 依赖版本为 `1.1.13.a` 以规避漏洞。

### Bug 修复

- (无)

### 功能移除

- (无)

---

## Release_1.0.1_20250510_build_A

### 功能构建

- 更新 README.md。

- Wiki 编写。
  - 构建 wiki 目录结构。
  - docs/wiki/en_US/Contents.md。
  - docs/wiki/en_US/Introduction.md。
  - docs/wiki/zh_CN/Contents.md。
  - docs/wiki/zh_CN/Introduction.md。

### Bug 修复

- (无)

### 功能移除

- (无)

---

## Release_1.0.0_20250509_build_A

### 功能构建

- 实现项目所有的预期功能。

- 项目结构建立，清理测试通过。

### Bug 修复

- (无)

### 功能移除

- (无)
